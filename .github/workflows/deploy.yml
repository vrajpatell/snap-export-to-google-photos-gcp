name: deploy

on:
  workflow_dispatch:

jobs:
  vercel-info:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Deployments are handled by Vercel Git integration."
          echo "If using Vercel CLI instead, configure VERCEL_TOKEN, VERCEL_ORG_ID, and VERCEL_PROJECT_ID."
