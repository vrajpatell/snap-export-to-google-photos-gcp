name: deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - run: |
          echo "This workflow is deprecated."
          echo "Use .github/workflows/deploy-cloud-run.yml for GCP CI/CD."
