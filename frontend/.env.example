# Required for Google Identity Services browser OAuth.
# Create this in Google Cloud Console as a Web application OAuth client and add
# your Vercel production/preview URLs to Authorized JavaScript origins.
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Optional legacy backend override. Leave empty for the free browser-only Vercel app.
VITE_API_BASE_URL=
