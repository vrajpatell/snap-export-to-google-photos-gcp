{
  "version": 2,
  "installCommand": "cd frontend && rm -rf node_modules && npm install && npm audit --audit-level=moderate",
  "buildCommand": "cd frontend && npm run test && npm run build",
  "outputDirectory": "frontend/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
